# Fabrik asset pipeline — Python dependencies
# Install with:  pip install -r requirements.txt
google-genai>=0.3.0     # Google Gen AI SDK ("from google import genai")
Pillow>=10.0.0          # image post-processing (crop, resize, save PNG)
numpy>=1.24.0           # fast chroma-key background removal
